<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.positionback.mapper.admin.AdminMapper">
    <!-- 일반 회원 목록 -->
    <select id="selectAllMembers" resultType="memberDTO">
        SELECT id,
        member_name,
        CASE
        WHEN member_email IS NOT NULL THEN member_email
        ELSE member_kakao_email
        END AS memberEmail,  <!-- memberEmail 필드에 두 컬럼 중 하나가 매핑 -->
        member_address,
        member_phone,
        member_status,
        created_date
        FROM tbl_member
    </select>

    <!-- 기업 회원 목록 -->
    <select id="selectAllCorporationMembers" resultType="corporationDTO">
        select id, corporation_name, corporation_address, corporation_address_detail, corporation_owner, corporation_email, corporation_password, corporation_homepage, corporation_gen, corporation_sales, corporation_code, corporation_employees_number, created_date from tbl_corporation
    </select>
</mapper>