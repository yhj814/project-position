<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.positionback.mapper.notice.NoticeMapper">
    <insert id="insertNotice">
        insert into tbl_notice (corporation_id, notice_title, notice_career, notice_education, notice_work_date, notice_end_date, job_categoryC_id)
        values (#{corporationId}, #{noticeTitle}, #{noticeCareer}, #{noticeEducation}, #{noticeWorkDate}, #{noticeEndDate},#{jobCategorycId})
    </insert>

</mapper>
